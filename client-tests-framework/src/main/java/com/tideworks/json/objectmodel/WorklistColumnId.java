package com.tideworks.json.objectmodel;


/**
 * Work List column name enum.
 */
public enum WorklistColumnId {
  containerNo,
  departBy,
  destination,
  from,
  goal,
  groupCode,
  hazard,
  lastFreeDay,
  origin,
  plan,
  shipper,
  sortCode,
  status,
  size,
  activationTime,
  to,
  weight
}
